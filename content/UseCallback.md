**Tags**: #react 

---

UseCallback is used to memoise callback functions that can be passed on to child component as for callbacks. 

What `useCallback` is not is that it's not useful for is to cache a callback that is to be attached to an event listener.
